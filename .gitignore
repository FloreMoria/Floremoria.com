# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.bak
*.tmp

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Variabili d'ambiente (segreti) — non versionare. Il template `.env.example` resta tracciato.
.env
.env.local
.env.production
.env.production.local
.env.development.local
.env.test.local
.env.evolution

# vercel
.vercel
env-per-vercel.txt
env.host.docker.example
*.txt
*.example
# Whitelist template senza segreti (da versionare)
!.env.example
!.env.evolution.example
# Permetti il tracciamento dei file di conoscenza di VERA
!/docs/whatsapp/knowledge_base_whatsapp.txt
!/docs/whatsapp/knowledge_base_whatsapp_examples.txt
!/docs/whatsapp/knowledge_base_whatsapp_core.txt

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

hydra-keys.env

# Dati leadgen / test di sviluppo (non in repository; usare storage interno se servono)
hydra_prospects.csv
hydra_completed.json
/test-stripe.js
/test-stripe2.js

# Originali foto ad alta risoluzione (solo locale; il manifest ignora già questa cartella)
public/images/products/_backup_originals/

# GA4 service account (mai in git)
/secrets/*.json
floremoria-*-*.json

# Archivio lean deploy (generato in locale, non versionare)
/floremoria-deploy.tar.gz

# PDF generati in locale (Script/export-volantino-pdf.mjs)
/volantino-b2b-floremoria.pdf
/volantino-b2b-fronte.pdf
/volantino-b2b-retro.pdf
.env*
