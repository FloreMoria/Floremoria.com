/** Imposta `false` per rimuovere il banner di manutenzione WhatsApp. */
export const WHATSAPP_MAINTENANCE_BANNER_ENABLED = false;

/** Altezza barra avviso — usata per offset navbar e contenuto. */
export const WHATSAPP_MAINTENANCE_BANNER_OFFSET = WHATSAPP_MAINTENANCE_BANNER_ENABLED
  ? '2.75rem'
  : '0px';
