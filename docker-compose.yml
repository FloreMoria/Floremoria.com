services:
  db:
    image: postgres:16-alpine
    container_name: floremoria_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: floremoria
      POSTGRES_PASSWORD: floremoria_pw
      POSTGRES_DB: floremoria
    ports:
      - "5432:5432"
    volumes:
      - floremoria_pgdata:/var/lib/postgresql/data

volumes:
  floremoria_pgdata:
