/**
 * @deprecated Motore legacy spento — usare `lib/financial/reconciliation.ts` (Neon DB).
 * Re-export di compatibilità per import residuali.
 */
export {
    reconcileTransaction,
    processManualOrders,
} from '@/lib/financial/reconciliation';
