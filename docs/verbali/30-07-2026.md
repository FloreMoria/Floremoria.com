# Verbale Operativo FloreMoria — 30 Luglio 2026 (Integrativo)

**Società:** FloreMoria S.r.l. (Startup Innovativa)  
**Redazione:** BARBARA (Staff AI) & Cursor / Antigravity  
**Ambiente:** Frontend Dashboard, MCP tooling, Production Vercel  
**Giornata di riferimento:** 2026-07-30.

## Stato sistemi (snapshot)

| Area | Status |
|------|--------|
| Production (`floremoria-dashboard`) | READY — ultimo deploy `dpl_8f6ZGV3Jqz7pnGdqnkBUfNvbQ5Vx` (commit `dfdfbe0`) |
| UI Giardino della Memoria | Fix spacing sotto navbar in production (`9a3e23d`) |
| MCP Obsidian Second Brain | Operativo |
| MCP GitHub | Operativo (`list_commits` verificato) |
| MCP Vercel | Operativo (`list_deployments` verificato) |
| MCP Postgres (locale) | Operativo — container `floremoria_db` avviato; 30 tabelle in `public` |

---

## Sezione 1 — Infrastruttura & Tooling AI

### Configurazione MCP Cursor (`~/.cursor/mcp.json`)

Attivati e verificati i server:

1. **obsidian-second-brain** — filesystem su `/Users/floremoria/Documents/Second Brain`
2. **github** — `@modelcontextprotocol/server-github` (lettura commit repo `FloreMoria/Floremoria.com`)
3. **vercel** — integrazione team `floremoria-srl-s-projects` (progetto produzione `floremoria-dashboard`)
4. **postgres** — `@modelcontextprotocol/server-postgres` → `localhost:5432/floremoria`

Nota operativa: al primo test Postgres il container era spento; avviato con `docker compose up -d db` (`floremoria_db`).

### Verifica produzione Vercel

- Progetto: **floremoria-dashboard** (serve `www.floremoria.com`)
- Ultimo deployment: stato **READY**, target **production**
- Commit tip: `dfdfbe0` — pipeline verbali Obsidian (github-actions)

---

## Sezione 2 — Sviluppo UI

### Fix Giardino della Memoria (dashboard utenti)

Commit: **`9a3e23d`**  
Messaggio: `fix(ui): sistemato spacing header Giardino della Memoria sotto la navbar e aggiornata dicitura intestazione`

1. **Overlap navbar:** overlay profilo `fixed` restava sotto la sticky nav per stacking context di `main` (z-10 vs z-30). Ora usa `top: var(--dashboard-chrome-h)` (navbar `h-14` ± banner Modalità Test).
2. **Nomenclatura:** «Scatola della Memoria Infinita» → **«Giardino della Memoria Infinita»**.
3. **Typecheck:** `npx tsc --noEmit` ok; push su `main`.

Verbale tecnico dedicato già in vault Obsidian: `2026-07-30_Fix_Giardino_Memoria.md`.

---

## Sezione 3 — Database locale (MCP)

Connessione confermata. Tabelle `public` elencate via MCP (estratto):

`Article`, `Category`, `DeceasedProfile`, `DeliveryProof`, `Order`, `OrderItem`, `Partner`, `Product`, `User`, `floremoria_logs`, `marketing_campaigns`, `whatsapp_chat_messages`, `whatsapp_chat_sessions`, … (totale **30** tabelle base).

---

## Commit di riferimento (giornata)

| SHA | Oggetto |
|-----|---------|
| `9a3e23d` | Fix UI Giardino della Memoria sotto navbar + dicitura |
| `dfdfbe0` | Ultimo deploy produzione (sync verbali automatico) |

---

## Note aperte

- Token MCP (GitHub/Vercel) restano in `mcp.json` locale: non commitare; preferibile rotazione periodica.
- Postgres MCP punta al DB **locale** Docker, non a Neon produzione.
