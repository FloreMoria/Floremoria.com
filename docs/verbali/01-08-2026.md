---
title: "Verbale Operativo Integrale — CRUD Defunti, Compensi Fiorista, Punto B e Anti-Duplicati VERA"
date: 2026-08-01
tags: [floremoria, verbale, dashboard, deceased, vera, whatsapp, pricing, blob, barbara, devin]
author: BARBARA (Staff AI) & Regia Centrale / Cursor (DEVIN)
---

# Verbale Operativo Integrale — 1 Agosto 2026

**Società:** FloreMoria S.r.l. (Startup Innovativa)  
**Redazione:** BARBARA (Staff AI) & Regia Centrale / Cursor (DEVIN)  
**Ambiente:** Dashboard Next.js / IDE Cursor / Production Vercel (`floremoria-dashboard`)  
**Giornata di riferimento:** 2026-08-01

---

## Sezione 1 — Dashboard Defunti (CRUD completo)

- **Full CRUD Scheda Defunto (`DeceasedProfile`):** introdotto `DeceasedProfileDrawer` con modalità modifica, toast di esito e dialog di conferma eliminazione.
- **API:** allineate route `/api/dashboard/deceased/[id]` (e alias `defunti`) per PUT/PATCH/DELETE/POST; sync date/tomba sugli ordini collegati.
- **Cover / profilo:** upload foto copertina via **Vercel Blob**; campo `coverUrl` sul profilo.
- **Migrazione Prisma:** applicata e deployata `20260801180000_deceased_cover_url`.
- **Commit di riferimento:** `6e741d4` — `feat(dashboard,vera): CRUD defunti, scadenza consegna fiorista e automazione verbali`.

---

## Sezione 2 — VERA operatività fiorista (scadenza consegna)

- Nuovo helper `lib/orders/formatFloristDeliveryDeadline.ts`: data limite da `deliveryDate` oppure fallback `createdAt + 48h`, formattata IT.
- Nel messaggio di incarico fiorista (Punto A) inserita riga `📅 CONSEGNA ENTRO:` subito sopra il luogo di consegna.
- Commit incluso in `6e741d4`.

---

## Sezione 3 — Listino compensi fiorista (rigido)

- Ripristinata la **tabella compensi rigida** da `FLOREM_NET_Catalogo_Prezzi_e_Link.txt` (niente percentuale 65% retail).
- Aggiornati `lib/pricing/listini.ts`, `calculateFloristCompensation.ts`, `floristRetailShare.ts`.
- Regole iniettate nei prompt VERA (`systemPrompt.ts`, `metodoFloremoria.ts`, `callerContext.ts`) così l’agent non inventa importi.
- **Commit:** `53ba648` — `fix(vera): listino compensi fiorista rigido e notifica presa in carico su ordini manuali`.

---

## Sezione 4 — Ordini manuali dashboard & Punto B (caso Valentina Cecchini)

- Ordini manuali dashboard con pagamento confermato: promozione **ACCEPTED/PENDING → IN_PROGRESS** per sbloccare presa in carico cliente.
- Punto B su creazione manuale: **bypass delay +30 min** (`bypassSchedule`) — non dipende dal wake Hobby.
- Fix template Meta `floremoria_conferma_ordine_utente`: da 3 a **2 body params** (errore `#132000`); warm/CTA in follow-up free-text.
- Reinviato con successo ordine **FT-RC-26-002** a Valentina Cecchini (+393478419867).
- **Commit:** `e47c667` — `fix(vera): allinea Punto B al template Meta a 2 parametri` (dopo `53ba648`).

---

## Sezione 5 — Anti-duplicati WhatsApp VERA

- Nuovo modulo `lib/whatsapp/veraWebhookDedup.ts`:
  - claim atomico **messageId Meta** (retry webhook non riprocessano);
  - lock outbound **25s** sullo stesso inbound;
  - claim intento conversazionale per conferme tipo *«Lunedì va benissimo»*.
- Webhook `/api/whatsapp/webhook`: metadata `whatsAppMessageId` / `replyToMessageId`; release lock se invio fallisce.
- Conferme data/cortesia: reply deterministica; fiorista + ordine `ACCEPTED` → `IN_PROGRESS` immediato.
- **Commit:** `3c9c440` — `fix(vera): blocca risposte WhatsApp duplicate su retry Meta`.
- Verifica: `npx tsc --noEmit` senza errori.

---

## Sezione 6 — Automazione verbali (giornata)

- Regola Cursor **Log Verbali Append Mode** (`.cursor/rules/log-verbali-append.mdc`) + LaunchAgent Mac per chiusura 23:50.
- Script `scripts/daily-verbale-cron.sh` (FDA `/bin/bash` richiesto su macOS).
- Pipeline Obsidian: `npm run log:verbale:sync-docs` → mirror `notes/obsidian/verbali/` + vault Second Brain.

---

## Commit Git rilevanti (2026-08-01)

| Hash | Ora | Messaggio |
|------|-----|-----------|
| `6e741d4` | 18:44 | feat(dashboard,vera): CRUD defunti, scadenza consegna fiorista e automazione verbali |
| `53ba648` | 19:19 | fix(vera): listino compensi fiorista rigido e notifica presa in carico su ordini manuali |
| `e47c667` | 19:31 | fix(vera): allinea Punto B al template Meta a 2 parametri |
| `3c9c440` | 19:56 | fix(vera): blocca risposte WhatsApp duplicate su retry Meta |

---

## Agenti coinvolti

| Agent | Ruolo nella giornata |
|-------|----------------------|
| **DEVIN** | CRUD defunti, pricing, webhook VERA, TypeScript |
| **PETRA** | Flusso Punto A/B, SLA notifica cliente, anti-loop |
| **VERA** | Messaggi fiorista/cliente, template Meta, conferme |
| **VITO** | Dedup/idempotenza webhook, no doppi invii |
| **NINA** | UX drawer defunti / dialog eliminazione |
| **BARBARA** | Verbale di chiusura e sync Obsidian |
| **SOFIA + ALMA** | Tono conferma cortesia / no spam sul dolore |

---

## Registro operativo automatico (.today_log)

Registro accumulato automaticamente da Cursor durante la giornata (fonte: `docs/verbali/.today_log.txt`).

- [2026-07-30 22:17] test-launchd
- [2026-08-01 18:13] feat(dashboard): CRUD completo defunti — DeceasedProfileDrawer (edit mode, delete confirm, toast), API /api/dashboard/deceased/[id] PUT/PATCH/DELETE/POST, coverUrl Blob, sync date/tomba su ordini.
- [2026-08-01 18:26] feat(vera): messaggio incarico fiorista con riga "📅 CONSEGNA ENTRO" (deliveryDate formattata IT, fallback createdAt+48h) subito sopra il luogo di consegna.
- [2026-08-01 19:19] feat(vera/pricing): tabella compensi fiorista rigida (no %); fix Punto B su ordine manuale PAID→IN_PROGRESS (presa in carico cliente).
- [2026-08-01 19:32] fix(vera): Punto B template floremoria_conferma_ordine_utente a 2 params (Meta #132000); warm/CTA come follow-up free-text; dashboard manuale bypassSchedule; reinviato FT-RC-26-002 a Valentina Cecchini.
- [2026-08-01 19:40] fix(vera): dedup webhook WhatsApp — claim atomico messageId Meta, lock outbound 25s per inbound, claim intento "Lunedì va benissimo"/conferme data (niente doppio Gemini), replyToMessageId in chat.

---

**Chiusura sessione:** verbale consolidato e pronto per sync Obsidian / push GitHub.
